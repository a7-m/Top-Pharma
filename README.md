# Top Pharma - المنصة التعليمية

منصة تعليمية احترافية لتعليم علم الصيدلة مع فيديوهات تعليمية، اختبارات تفاعلية، وملفات دراسية.

## 📋 المميزات

- ✅ **نظام تسجيل وحماية**: تسجيل دخول آمن مع حماية الصفحات
- 🎥 **فيديوهات تعليمية**: محاضرات، مراجعات، وتطبيقات عملية
- 📝 **اختبارات تفاعلية**: اختبارات مع تصحيح تلقائي وحفظ النتائج
- 📚 **ملفات ومراجع**: تحميل ملفات PDF ومواد دراسية
- 📱 **تصميم متجاوب**: يعمل على جميع الأجهزة

## 🛠️ التقنيات المستخدمة

- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Backend**: Supabase (PostgreSQL + Authentication + Storage)
- **التصميم**: تصميم طبي احترافي بألوان هادئة

## 🚀 الإعداد والتشغيل

### 1. متطلبات التشغيل

- حساب Supabase (مجاني)
- متصفح ويب حديث
- خادم ويب محلي (اختياري للتطوير)

### 2. إعداد Supabase

#### أ. إنشاء المشروع

1. سجل دخول إلى [Supabase](https://supabase.com)
2. أنشئ مشروعاً جديداً
3. انسخ بيانات الاعتماد (موجودة بالفعل في `js/config.js`)

#### ب. تنفيذ قاعدة البيانات

1. افتح SQL Editor في Supabase Dashboard
2. انسخ محتوى ملف `database-schema.sql`
3. نفذ الأوامر (Run)
4. تأكد من إنشاء جميع الجداول بنجاح

#### ج. إنشاء Storage Buckets

**مهم جداً:** يجب إنشاء هذه المجلدات يدوياً:

1. اذهب إلى Storage في Supabase Dashboard
2. أنشئ Bucket جديد بإسم `videos`:
   - اجعله Public
   - File size limit: 100MB (أو حسب الحاجة)
3. أنشئ Bucket جديد بإسم `files`:
   - اجعله Public
   - File size limit: 10MB

#### د. إعداد Storage Policies

لكل Bucket، أضف هذه Policies:

```sql
-- للقراءة (Read)
CREATE POLICY "Allow authenticated users to read"
ON storage.objects FOR SELECT
USING (bucket_id = 'videos' AND auth.role() = 'authenticated');

-- للرفع (Upload - للمشرفين فقط)
CREATE POLICY "Allow admins to upload"
ON storage.objects FOR INSERT
WITH CHECK (bucket_id = 'videos' AND auth.role() = 'authenticated');
```

كرر نفس الخطوات للـ `files` bucket.

### 3. تشغيل المشروع محلياً

#### استخدام Python HTTP Server:

```bash
python -m http.server 8000
```

#### أو استخدام PHP:

```bash
php -S localhost:8000
```

#### أو استخدام Live Server (VS Code):

1. ثبت امتداد "Live Server"
2. انقر بزر الماوس الأيمن على `index.html`
3. اختر "Open with Live Server"

### 4. اختبار المنصة

1. افتح المتصفح على `http://localhost:8000`
2. انقر على "إنشاء حساب جديد"
3. سجل حساباً جديداً
4. سجل دخول وتصفح المنصة

## 📁 هيكل المشروع

```
Top Pharma/
├── index.html              # الصفحة الرئيسية
├── login.html              # صفحة تسجيل الدخول
├── register.html           # صفحة التسجيل
├── dashboard.html          # لوحة التحكم
├── videos.html             # صفحة الفيديوهات
├── video-player.html       # مشغل الفيديو
├── quizzes.html            # صفحة الاختبارات
├── quiz-take.html          # صفحة حل الاختبار
├── files.html              # صفحة الملفات
├── css/
│   └── style.css          # التنسيقات الرئيسية
├── js/
│   ├── config.js          # إعدادات Supabase
│   ├── supabase.js        # عميل Supabase
│   ├── auth.js            # خدمة المصادقة
│   ├── videos.js          # خدمة الفيديوهات
│   ├── quizzes.js         # خدمة الاختبارات
│   ├── files.js           # خدمة الملفات
│   └── utils.js           # دوال مساعدة
├── database-schema.sql     # مخطط قاعدة البيانات
└── README.md              # هذا الملف
```

## 🗄️ قاعدة البيانات

### الجداول الرئيسية:

- **profiles**: بيانات المستخدمين
- **videos**: معلومات الفيديوهات
- **quizzes**: الاختبارات
- **questions**: أسئلة الاختبارات
- **quiz_attempts**: محاولات الطلاب
- **files**: الملفات والمراجع

## 📤 رفع المحتوى

### رفع فيديوهات:

1. اذهب إلى Supabase Dashboard → Storage → videos
2. انقر على "Upload file"
3. ارفع الفيديو
4. انسخ الـ path
5. أضف سجلاً جديداً في جدول `videos` مع الـ path

مثال SQL:

```sql
INSERT INTO videos (title, description, video_url, category, duration)
VALUES (
    'عنوان الفيديو',
    'وصف الفيديو',
    'path/to/your-video.mp4',
    'lecture',
    3600
);
```

### رفع ملفات:

1. اذهب إلى Storage → files
2. ارفع الملف
3. أضف سجلاً في جدول `files`

## 🎨 التخصيص

### تغيير الألوان:

عدّل متغيرات CSS في `css/style.css`:

```css
:root {
  --primary-color: #2c5aa0; /* اللون الأساسي */
  --secondary-color: #27ae60; /* اللون الثانوي */
}
```

## 🔐 الأمان

- ✅ Row Level Security (RLS) مفعّل على جميع الجداول
- ✅ حماية الصفحات من الوصول غير المصرح
- ✅ التحقق من البيانات في الـ Frontend والـ Backend
- ✅ استخدام JWT tokens آمنة

## 📱 التوافق

- ✅ Chrome, Firefox, Safari, Edge
- ✅ الهواتف الذكية والأجهزة اللوحية
- ✅ شاشات مختلفة (من 320px إلى 1920px+)

## 🐛 حل المشاكل

### المستخدم لا يستطيع التسجيل:

- تأكد من تنفيذ `database-schema.sql` بالكامل
- تحقق من وجود trigger `handle_new_user`

### الفيديوهات لا تظهر:

- تأكد من وجود bucket `videos` في Storage
- تحقق من RLS policies على جدول `videos`
- راجع Console للأخطاء

### الملفات لا تُحمّل:

- تأكد من bucket `files` موجود وعام (Public)
- تحقق من صحة الـ file_url في قاعدة البيانات

## 📞 الدعم

للأسئلة والدعم التقني:

- البريد الإلكتروني: [بريدك هنا]
- أو افتح Issue في GitHub

## 📄 الترخيص

جميع الحقوق محفوظة © 2026 
Top Pharma Platform - منصة تعليمية متخصصة

---

**ملاحظة**: هذا المشروع للأغراض التعليمية. تأكد من مراجعة وتحديث إعدادات الأمان قبل النشر للإنتاج.
